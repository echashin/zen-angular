export type date_format = 'yyyy-MM-dd';
export type time_format = 'HH:mm';
export type date_time_format = `${date_format} ${time_format}`;

export const DATE_FORMAT: date_format = 'yyyy-MM-dd';
export const TIME_FORMAT: time_format = 'HH:mm';
export const DATE_TIME_FORMAT: date_time_format = 'yyyy-MM-dd HH:mm';
